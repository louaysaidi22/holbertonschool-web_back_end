#!/usr/bin/env python3
"Complex types - list of floats"


def sum_list(input_list: list[float]) -> float:
    """sum_list is a type-annotated function that takes:
        - a list of float arguments
       and returns:
        - their sum as a float"""
    return sum(input_list)
