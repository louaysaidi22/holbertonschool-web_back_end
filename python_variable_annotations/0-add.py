#!/usr/bin/env python3
"Basic annotations - add"


def add(a: float, b: float) -> float:
    """add is a type-annotated function that takes two arguments:
        -float a
        -float b
       and returns:
        their sum as a float."""
    return a + b
