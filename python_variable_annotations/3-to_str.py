#!/usr/bin/env python3
"Basic annotations - to string"


def to_str(n: float) -> str:
    """add is a type-annotated function that takes one argument:
        - float n
       and returns:
        - the string representation of the float"""
    return str(n)
